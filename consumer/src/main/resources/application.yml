spring:
  application:
    name: CONSUMER


server:
  port: 80

eureka:
  client:
    service-url:
      ##ע������
      defaultZone: http://localhost:1111/eureka,http://localhost:1112/eureka,http://localhost:1113/eureka
    fetch-registry: false #不能向注册中心注册自己